export interface Book {
    _id: string,
    average_rating: number,
    title: string,
    authors: string,
    publisher: string,
    publication_date: string,
    num_pages: number,
    price: number
}